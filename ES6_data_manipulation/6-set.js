export default function setFromArray(array) {
  /* Create and return a new Set containing the elements of the given array.
  Duplicates in the table are automatically removed
  Because a Set can only contain unique values. */
  return new Set(array);
}
